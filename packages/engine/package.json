{
  "name": "@regolith-rail/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p . && tsc -p tsconfig.test.json && tsc -p scripts",
    "generate:schema": "node scripts/generate-schema.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.4",
    "ajv": "^8.20.0",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "zod": "4.6.4"
  }
}
